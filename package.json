{
  "scripts": {
    "lint": "concurrently \"yarn:lint-*\""
  },
  "devDependencies": {
    "@commitlint/cli": "^16.0.1",
    "@commitlint/config-conventional": "^16.0.0",
    "concurrently": "^7.0.0",
    "husky": "^7.0.4",
    "run.env": "^1.1.0"
  }
}
